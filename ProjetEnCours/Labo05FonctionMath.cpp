#include "Labo05FonctionMath.h"
// But : écrire le code des fonctions pour créer la définition de chaque fonction
// Auteur : 
// Date : 

// Ici le paramètre valeur est une variable qui est déjà initialisée
double calculerCarre(double valeur)
{
   // dans une fonction on peut tout faire comme dans le main
   // Déclaration des variables
   double resultat;

   resultat = valeur * valeur;

   // La fonction doit retourner le résultat du calcul pour le partager avec d'autres
   return resultat;
   // return valeur*valeur;
}

